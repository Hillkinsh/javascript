首先，有一段很精炼的代码
var global = (function(){
    return this || (0,eval)('this');
    
}())
这段代码是为了获取当前上下文。
那么很明显的，这段代码可以拆分如下：
var tempFunc = function(){
    if(this){
        return this;
    }else{
        return (0,eval)('this');
    }
}

var global = tempFunc();

这就很清晰了。在这里(0,eval)返回的是eval函数，至于为什么要用这么个表达式，
而不是直接用呢？主要考虑还是一个ie的低版本兼容问题。
以上
