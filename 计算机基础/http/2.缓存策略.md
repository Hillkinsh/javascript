# 缓存策略

浏览器在发送请求时，会先获取该资源的缓存的head信息。然后从cache-control 和 expires 判断是否过期。
若没过期，直接拿来使用，不会发送网络请求。

强缓存
expires和cache-control的区别
1. 绝对的gmt时间字符串，表示资源的失效时间，当服务器和客户端时间偏差较大时，导致缓存混乱
2. 一般使用max-age=3600字段来判断。相对时间。优先级高。

如果缓存过期，会带着有关缓存的head信息发送请求。
etag if-none-match

